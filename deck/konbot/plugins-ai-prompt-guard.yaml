_format_version: "3.0"
_transform: true
_konnect:
  control_plane_name: kubernetes-gateway

plugins:
  - name: ai-prompt-guard
    service: chat-service
    config:
      deny_patterns:
        - ".*バナナ.*"
