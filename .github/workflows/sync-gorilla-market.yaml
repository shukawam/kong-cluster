name: Sync decK config(Gorilla Market)

on:
  push:
    paths:
      - deck/global/*.yaml
      - deck/gorilla-market/*.yaml
      - .github/workflows/sync-gorilla-market.yaml
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v5
      - name: Setup deck
        uses: kong/setup-deck@v1
        with:
          deck-version: 1.49.0
      - name: Add tag
        run: |
          deck file add-tags gorilla-market \
            -s deck/gorilla-market/kong.yaml \
            -o deck/gorilla-market/kong.yaml
      - name: Diff current environment
        run: |
          deck gateway diff deck/gorilla-market/merged.yaml \
            --select-tag gorilla-market \
            --konnect-token ${{ secrets.KONNECT_TOKEN }}
      - name: Sync gateway config
        run: |
          deck gateway sync deck/gorilla-market/merged.yaml \
            --select-tag gorilla-market \
            --konnect-token ${{ secrets.KONNECT_TOKEN }}
